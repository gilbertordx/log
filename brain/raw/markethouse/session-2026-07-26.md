# MarketHouse Session (2026-07-26)

## Features & Progress
- **API Protection**: Implemented global `AuthGuard` + `RolesGuard` on NestJS backend (`feat/proteger-rotas-api`).
- **Access Control**: Configured role-based route protection across controllers (`roles.guard.ts`, `setor.util.ts`, `itens.controller.ts`).
- **Documentation**: Merged PR #59 (`docs/regras-de-fluxo`) updating project context and flow rules.
